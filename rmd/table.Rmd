---
output:
  pdf_document: default
---


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE,
                      message = FALSE,
                      warning = FALSE)
```

```{r table, results='asis'}
pacman::p_load(tidyverse)
df_parms <- readRDS(here::here("data_fmt/data_parms.rds"))

knitr::kable(df_parms,
                booktabs = TRUE,
                escape = FALSE,
                format = "simple",
                caption = "Parameter values in analytical and numerical analysis.")
```

